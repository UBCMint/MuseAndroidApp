<?xml version="1.0" encoding="utf-8"?>

<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="ca.ubc.best.mint.museandroidapp.RecordingActivity">

    <data>
        <import type="android.view.View"/>

        <variable name="activity" type="ca.ubc.best.mint.museandroidapp.RecordingActivity" />
        <variable name="recordVM" type="ca.ubc.best.mint.museandroidapp.vm.RecordingViewModel" />
    </data>


    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/scanButton"
            android:visibility="@{recordVM.hasDevice() ? View.GONE : View.VISIBLE}"
            android:text='@{recordVM.canStartScan() ? "Scan for Device" : "Scanning"}'
            android:enabled="@{recordVM.canStartScan()}"
            android:onClick="@{() -> activity.handleScanClicked()}"
            android:layout_width="128dp"
            android:layout_height="64dp"
            android:layout_marginRight="8dp"
            android:layout_marginLeft="8dp"
            android:layout_marginTop="8dp"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            />

        <TextView
            android:id="@+id/connectionStrengthText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:layout_marginRight="8dp"
            android:layout_marginTop="8dp"
            android:gravity="right"
            android:text="Connection Strength:"
            android:visibility="@{recordVM.hasDevice() ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="@+id/connectionStrength"
            app:layout_constraintRight_toLeftOf="@+id/connectionStrength"
            app:layout_constraintTop_toTopOf="@+id/connectionStrength"
            app:layout_constraintVertical_bias="0.733"
            android:layout_marginLeft="8dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintHorizontal_bias="1.0"/>

        <eeg.useit.today.eegtoolkit.view.ConnectionStrengthView
            android:id="@+id/connectionStrength"
            app:connectionStrength="@{recordVM.getConnectionStrength()}"
            android:visibility="@{recordVM.hasDevice() ? View.VISIBLE : View.GONE}"
            android:layout_width="128dp"
            android:layout_height="64dp"
            android:layout_marginRight="8dp"
            android:layout_marginLeft="8dp"
            android:layout_marginTop="8dp"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            />

        <android.support.constraint.ConstraintLayout
            android:id="@+id/recordSection"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginBottom="8dp"
            android:layout_marginLeft="8dp"
            android:layout_marginRight="8dp"
            android:layout_marginTop="72dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.0">

            <TextView
                android:id="@+id/textView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="8dp"
                android:layout_marginTop="8dp"
                android:text="To Record:"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toTopOf="parent"/>

            <CheckBox
                android:id="@+id/useRaw"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="8dp"
                android:layout_marginTop="8dp"
                android:text="Raw Data"
                android:checked="@{recordVM.isRawSelected()}"
                android:onClick="@{recordVM::onClickRaw}"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/textView"/>

            <CheckBox
                android:id="@+id/useConnection"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="8dp"
                android:layout_marginTop="8dp"
                android:text="Connection Strength"
                android:checked="@{recordVM.isConnectionSelected()}"
                android:onClick="@{recordVM::onClickConnection}"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/useRaw"/>

            <CheckBox
                android:id="@+id/useRelativeAlpha"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="8dp"
                android:layout_marginTop="8dp"
                android:text="Alpha frequency"
                android:checked="@{recordVM.isAlphaSelected()}"
                android:onClick="@{recordVM::onClickAlpha}"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/useConnection"/>

            <Button
                android:id="@+id/recordButton"
                android:enabled="@{recordVM.canStartRecording() || recordVM.canStopRecording()}"
                android:text='@{recordVM.canStopRecording() ? "Stop recording" : "Record"}'
                android:onClick="@{() -> activity.handleRecordClicked()}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="8dp"
                android:layout_marginRight="8dp"
                android:layout_marginTop="32dp"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/useRelativeAlpha"/>

        </android.support.constraint.ConstraintLayout>
    </android.support.constraint.ConstraintLayout>

</layout>
